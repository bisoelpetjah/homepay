import React, { FC } from 'react'
import { ScrollView, Text } from 'react-native'

const DashboardInteriorDesignFirm: FC = () => {
  return (
    <ScrollView>
      <Text>Interior design firm</Text>
    </ScrollView>
  )
}

export default DashboardInteriorDesignFirm
